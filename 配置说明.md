# Charger Tag 与 Attribute 说明

本文档根据当前 `src` 源码整理，说明项目中已有的 CollectionService Tag、对应 Attribute、配置范围、用途，以及常用玩法常量所在的脚本。Tag、Attribute 和大部分全局平衡参数的代码来源为 `src/shared/GameplayConfig.luau`。

## Tag 配置速查表

配置人员可以先按此表完成对象、Tag 和 Attribute，再到后续对应章节查看行为细节。Attribute 名区分大小写，类型必须完全匹配。

| Tag | 放置位置与接受对象 | Attribute | 属性类型 | 谁来配置 | 默认值 / 有效范围 | 用途 |
| --- | --- | --- | --- | --- | --- | --- |
| `GameZone` | Workspace；`BasePart` 或含 Part 的 `Model` | `ZoneIndex` | Number（整数） | **策划必填** | `0..1,000,000` | 区域顺序编号及拾取物预生成依据 |
| `GameZone` | 同上 | `MinionSpawnCount` | Number（整数） | 策划可选 | `-1`，或 `1..1,000`；默认 `10` | `-1` 表示该区域不生成拾取物；正数表示每名玩家的生成数量 |
| `GameZone` | 同上 | `ForcedRunSpeed` | Number | 策划可选 | 不设置或 `0` 表示关闭；启用值 `> 0` 且 `<= 200` | 玩家在该区域内的固定前进速度 |
| `GameZone` | 同上 | `ForcedRunDirection` | Vector3 | 启用强制前进时**策划必填** | X/Z 水平分量不能同时为 0；Y 分量忽略 | 固定前进的世界方向，运行时归一化 |
| `Minion` | `ServerStorage`；`BasePart` 或含 Part 的 `Model` | `EnergyValue` | Number | **策划必填** | `1..1,000,000` | 该兵种代表的战力；`Minion` 是旧版模板发现 Tag |
| `Minion` | 同上 | `EnergyUpgradePerLevel` | Number（整数） | **策划必填** | `1..1,000,000` | 该人偶每购买 1 级商店能量升级所增加的能量 |
| `Minion` | 同上 | `MinionIconId` | String 或 Number（整数） | **策划必填** | 纯数字 Roblox 图片资源 ID | 该人偶在商店卡片中显示的图标；不要填写资源前缀 |
| `Minion` | 同上 | `MinionVisualMode` | String | 策划可选 | `Auto`、`Animated`、`Static`；默认 `Auto` | 控制跟随士兵客户端视觉模式 |
| `Minion` | 同上 | `FacingYawDegrees` | Number | 策划可选 | `-3600..3600`；默认 `0` | 修正跟随士兵朝向 |
| `Minion` | 同上 | `GroundOffsetStuds` | Number | 策划可选 | `-20..20`；默认 `0` | 修正跟随士兵贴地高度 |
| `MinionPicker` | 服务端生成，或作为 `GameZone` 后代预置；`BasePart` 或含 Part 的 `Model` | `MinionType` | String | 自动生成时由程序写入；预置对象可选 | 预置缺省/空字符串时按玩家随机已解锁兵种；非空时精确匹配模板名 | 决定拾取后加入的兵种 |
| `MinionPicker` | 同上 | `SpawnOwnerUserId` | Number（整数） | 程序运行时写入 | 玩家 `UserId`；不设置则不限制 | 只允许指定玩家拾取 |
| `MinionPicker` | 同上 | `Collected` | Boolean | 程序运行时写入 | 默认 `false` | 防止多个身体部位重复领取 |
| `MinionModifier` | Workspace；仅 `BasePart` | `Operation` | String | **策划必填** | `Add`、`Subtract`、`Multiply` | 指定士兵数量运算 |
| `MinionModifier` | 同上 | `Operand` | Number（整数） | **策划必填** | 加减 `1..1,000`；乘法 `0..10` | 运算使用的数量或倍数 |
| `MinionModifier` | 同上 | `Enabled` | Boolean | 策划可选 | 默认 `true` | 是否允许触发并显示文字 |
| `RandomMinionModifierGroup` | Workspace；仅 `Model` | （无） | — | 无需配置 Attribute | 至少 2 个直接子级 BasePart | 程序为每名玩家、每次出生生成一正一负的随机门 |
| `Trap` | Workspace；`BasePart` 或含 Part 的 `Model` | `EnergyDamage` | Number | **策划必填** | `1..1,000,000` | 单次触碰最多扣除的战力 |
| `KillZone` | Workspace；仅 `BasePart` | （无） | — | 无需配置 Attribute | 无 | 玩家角色触碰后立即死亡 |
| `Boss` | Workspace；`BasePart` 或含 Part 的 `Model` | `RequiredEnergy` | Number | **策划必填** | `1..1,000,000` | 击败 Boss 所需并消耗的战力 |
| `Boss` | 同上 | `BossOwnerUserId` | Number（整数） | 程序运行时写入 | 所属玩家 `UserId` | 隔离每名玩家的 Boss 副本与触碰判定 |
| `Boss` | 同上 | `Defeated` | Boolean | 程序运行时写入 | 默认 `false` | 防止 Boss 被重复结算 |
| `Shop` | Workspace；`BasePart` 或含 Part 的 `Model` | （无） | — | 策划可选 | 名称为 `Shop` 的对象也会自动绑定 | 玩家靠近后按 `E` 打开商店界面 |
| `EnergyCapacityUpgrade` | Workspace；仅 `BasePart` | （无） | — | 无需配置 Attribute | 必须启用 `Anchored`、`CanCollide`、`CanTouch`，并包含 `SurfaceGui/TextLabel` | 每名玩家看到自己的当前升级价格；持续站立时按配置间隔自动购买能量上限 |
| `Treadmill` | Workspace；包含名为 `Conveyor` 的 BasePart 的 `Model` | `TreadmillEnabled` | Boolean | 策划可选 | 默认 `true` | 是否启用跑步机奖励和购买提示 |
| `Treadmill` | 同上 | `TreadmillSubscriptionId` | String | 高倍率跑步机可选 | `EXP-` 加纯数字；不设置时读取共享配置 | 覆盖该模型对应的月度订阅 ID |
| `Treadmill` | 同上 | `TreadmillGamePassId` | Number（整数） | 高倍率跑步机可选 | 正整数；不设置时读取共享配置 | 覆盖该模型对应的永久解锁 Game Pass ID |
| `FinalZone` | Workspace；仅 `BasePart` | （无） | — | 无需配置 Attribute | 必须启用 `Anchored`、`CanTouch` | 触发约 2 秒庆祝音效与动画，随后清空跟随人偶和当前能量并传回出生点 |
| `TestEnergyReward` | Workspace；仅开发期自动生成的 `BasePart` | （无） | — | 无需配置 Attribute | 每次触碰将当前能量和本局上限设为 `5,000` | 独立测试能量；绝不添加跟随小兵 |

### 对象通用要求

- Tag 与策划配置的 Attribute 应放在表中指定的功能根对象上。
- `MinionModifier` 与 `KillZone` 的功能根必须是 `BasePart`；`RandomMinionModifierGroup` 的功能根必须是 `Model`。
- 其他支持 Model 的场景 Tag 可放在 `BasePart`，或包含至少一个 BasePart 的根 `Model`。
- 触碰类对象的相关 Part 必须允许触碰事件；门和触发器通常设置 `CanCollide = false`。
- 表中标为“程序运行时写入”的 Attribute 不需要策划手工创建，也不应作为关卡配置修改。

## 脚本常量与全局平衡配置

### 主要配置入口

| 脚本 | 配置表/常量 | 适合调整的内容 |
| --- | --- | --- |
| `src/shared/GameplayConfig.luau` | `GameplayConfig.Defaults` | 战力容量、跑动战力、静止消耗、拾取物数量与间距、陷阱冷却、演示 Boss 默认值 |
| `src/shared/GameplayConfig.luau` | `GameplayConfig.MinionVisual` | 跟随士兵外观模式、朝向偏移和贴地偏移的取值范围 |
| `src/shared/GameplayConfig.luau` | `GameplayConfig.MinionPickup` | 小兵拾取反馈音效 |
| `src/shared/GameplayConfig.luau` | `GameplayConfig.Boss` | Boss 触碰容差、目标提示和击败碎裂表现 |
| `src/shared/GameplayConfig.luau` | `GameplayConfig.Final` | 终点庆祝音效、R6/R15 动画和结算延迟 |
| `src/shared/GameplayConfig.luau` | `GameplayConfig.Progression` | 等级/经验曲线与存档参数；当前跑动加经验和经验条显示已停用 |
| `src/shared/GameplayConfig.luau` | `GameplayConfig.Limits` | 等级、战力数值、区域编号、小兵数量等安全上限 |
| `src/shared/ShopConfig.luau` | `ShopConfig` | 游戏币跑动奖励、商店价格曲线、小兵解锁、订阅和兼容 Game Pass 配置 |
| `src/shared/SubscriptionConfig.luau` | `SubscriptionConfig` | 跑步机倍率与 cat/dog 对应的 Experience Subscription ID |
| `src/shared/MinionModifierConfig.luau` | `MinionModifierConfig`、`RandomGroup` | 手工数量门范围、触碰距离、随机门效果池和额外门正面概率 |
| `src/shared/TreadmillConfig.luau` | `TreadmillConfig` | 跑步机名称倍率、每秒 Coins、付费倍率门槛、订阅/兼容 Game Pass 与购买提示 |
| `src/client/MinionPresentation.client.luau` | 文件顶部的本地常量 | 小兵队形、跟随延迟、跑步循环、入队和击飞动画 |
| `src/client/EnergyHud.client.luau` | `CAMERA_*` | Boss 撞击时的镜头拉远、FOV 和各阶段时长 |

通常优先修改 `src/shared` 中的集中配置。服务脚本或客户端脚本里的本地常量属于实现/表现细节；修改前应确认不会破坏碰撞验证、生命周期或性能。

### 战力、容量与区域默认值

以下字段位于 `src/shared/GameplayConfig.luau` 的 `GameplayConfig.Defaults`：

| 字段 | 当前值 | 单位/公式 | 实际用途 |
| --- | ---: | --- | --- |
| `PlayerLevel` | `1` | 等级 | 玩家等级 Attribute 不存在时的后备等级 |
| `PlayerRespawnSeconds` | `0.5` | 秒 | 玩家死亡后的全局重生间隔；启动时写入 `Players.RespawnTime` |
| `BaseCapacity` | `120` | 战力 | 1 级玩家的战力上限 |
| `CapacityPerLevel` | `100` | 战力/级 | 每升 1 级增加的战力上限 |
| `EnergyPerStud` | `0.1` | 战力/stud | 每个完整跑动距离结算段按距离换算的战力 |
| `DistancePerEnergyAward` | `10` | studs/次 | 累计满 10 studs 才结算一份跑动战力，未满部分留到后续跑动 |
| `IdleEnergyDrainPerSecond` | `5` | 战力/秒 | 玩家处于 `GameZone` 但没有有效跑动时的战力消耗 |
| `MinionSpawnCount` | `10` | 个/区域 | 未设置或填写无效值时的拾取物数量；场景填 `-1` 可禁止该区域生成 |
| `MinionSpawnMinSpacing` | `5` | stud | 同一玩家同一区域的拾取物最小间距 |
| `TrapTouchCooldownSeconds` | `1` | 秒 | 同一玩家触发同一个陷阱的服务端冷却 |
| `MinionEnergyValue` | `10` | 战力 | 当前为保留默认值；实际值读取士兵模板的 `EnergyValue` Attribute |
| `BossRequiredEnergy` | `100` | 战力 | 当前为保留默认值；正式 Boss 读取自身 `RequiredEnergy` Attribute |

当前战力上限不再随玩家等级增长，公式为：

```text
EnergyCapacity = BaseCapacity + EnergyCapacityUpgrade 区域购买的容量升级加成
```

按当前基础配置：未购买商店容量升级时，上限固定为 120。玩家在 GameZone 内每累计跑满 10 studs 结算 `10 × 0.1 = 1` 战力；一次移动跨过多个完整结算段时会结算全部份数，未满 10 studs 的剩余距离保留到后续跑动，角色重建时清零。战力最终始终截断在 `0..EnergyCapacity`。

### 商店、游戏币与小兵升级

以下字段位于 `src/shared/ShopConfig.luau`：

| 字段 | 当前值 | 用途 |
| --- | ---: | --- |
| `DataStoreName` | `PlayerShop_v1` | 游戏币、容量升级、小兵解锁和小兵能量升级存档 |
| `DefaultUnlockedMinionType` | `rabbit` | 新玩家默认解锁的小兵模板名 |
| `CoinRewards.DistancePerAward` | `100` | 在 GameZone 内累计多少有效跑动距离结算一次 Coins |
| `CoinRewards.CoinsPerAward` | `5` | 每次跑动距离结算奖励的 Coins |
| `MinionEnergyUpgrades.BaseCost` | `50` | 小兵能量升级默认初始价格 |
| `MinionEnergyUpgrades.CostMultiplier` | `1.35` | 小兵能量升级价格倍率 |
| `MinionEnergyUpgrades.MaxLevel` | `100` | 每个兵种能量升级最高等级 |
| `EnergyCapacityUpgrades.CapacityPerLevel` | `10` | 玩家每次踏上升级区域成功购买后增加的能量上限 |
| `EnergyCapacityUpgrades.AutoPurchaseIntervalSeconds` | `0.75` | 玩家持续站在升级区域时自动重复购买的时间间隔（秒） |
| `EnergyCapacityUpgrades.BaseCost` | `10` | 容量升级初始价格 |
| `EnergyCapacityUpgrades.CostMultiplier` | `1.4` | 容量升级价格倍率 |
| `EnergyCapacityUpgrades.MaxLevel` | `100` | 容量升级最高等级 |
| `MinionUnlocks.DefaultCoinCost` | `500` | 非默认小兵未单独配置时的 Coins 解锁价格 |

`ShopConfig.Minions` 可按小兵模板名称覆盖商店展示名、Coins 解锁价格、升级基础价格、`gamePassId` 和 `subscriptionId`。这里的“小兵模板名称”指表项的外层 key，而不是内部字段；外层 key 必须与 `ServerStorage/MinionTemplates` 下的模板对象名称完全一致，包括大小写和拼写。`displayName` 只用于商店界面展示，不参与生成、拾取或解锁匹配。`gamePassId` 大于 0 时显示永久 Robux 解锁入口；`subscriptionId` 为有效 `EXP-<digits>` 时显示 Robux 订阅入口。Coins 或 Game Pass 解锁会永久保存，订阅解锁只在服务端确认 `IsSubscribed` 的有效期内生效，不写入 DataStore。

每个 `Minion` 人偶模板还必须在根对象上配置 `EnergyUpgradePerLevel`（整数 `1..1,000,000`）。这项属性决定该人偶在商店每升 1 级增加的能量；不同人偶可以填写不同值。例如兔子填 `2`、士兵填 `10`。缺失或无效时，该兵种不会出现在商店中，且服务端拒绝其升级结算。

每个模板还应配置 `MinionIconId`，可使用纯数字 String 或正整数 Number，只填写 Roblox 图片资源的数字 ID，不要填写 `rbxassetid://` 前缀。服务端会将它转换为商店可用的图片地址；缺失或无效时会输出一次警告，并让该人偶的商店图标保持为空，但不会影响人偶解锁、升级或生成。

例如当前模板结构为：

```text
ServerStorage
└─ MinionTemplates
   ├─ ToySolider
   └─ rabbit
```

则商店配置应写成：

```lua
ShopConfig.Minions = {
	rabbit = {
		displayName = "Rabbit",
		unlockCoinCost = 0,
		upgradeBaseCost = 50,
		gamePassId = 0,
	},

	ToySolider = {
		displayName = "Toy Soldier",
		unlockCoinCost = 500,
		upgradeBaseCost = 75,
		gamePassId = 0,
	},
}
```

注意：如果 Studio 里的模板对象名是 `ToySolider`，配置 key 也必须写 `ToySolider`；如果将模板改名为 `ToySoldier`，配置 key、手摆 `MinionPicker.MinionType`、数量门 `MinionType` 也应同步改为 `ToySoldier`。

小兵拾取、数量门和随机生成都会经过服务端解锁检查：未解锁兵种不会作为随机拾取物生成，未解锁的手摆预置会被跳过并尽量用已解锁随机拾取物补足数量，客户端伪造未解锁兵种也不会成功加入队伍。

### 商店场景入口与按 E 打开

商店界面由服务端 `PlayerShopService` 创建的 `ProximityPrompt` 激活。服务启动时会自动绑定两类场景对象：

- `Workspace` 下名称精确为 `Shop` 的 `BasePart` 或包含 Part 的 `Model`。
- 任意带 `Shop` Tag 的 `BasePart` 或包含 Part 的 `Model`。

当前场景里的入口是：

```text
Workspace
└─ town
   └─ Shop
```

因此它即使暂时没有 `Shop` Tag，也会因为模型名是 `Shop` 被自动加上交互提示。玩家靠近后按 `E`，服务端会触发 `ShopOpenRequested` RemoteEvent，客户端 `ShopController.client.luau` 收到后拉取最新商店快照并打开商店界面。

交互提示参数集中在 `src/shared/ShopConfig.luau` 的 `ShopConfig.Prompt`：

```lua
ShopConfig.Prompt = {
	Name = "ShopPrompt",
	ActionText = "Open",
	ObjectText = "Shop",
	KeyboardKeyCode = Enum.KeyCode.E,
	MaxActivationDistance = 12,
	HoldDuration = 0,
}
```

如果以后把场景里的商店模型改名，建议给模型添加 `Shop` Tag，或保持模型名仍为 `Shop`。绑定对象必须是 `BasePart`，或是内部至少包含一个 `BasePart` 的 `Model`；Prompt 会挂到该对象的主 Part 上。

### EnergyPart 能量上限升级

场景中的 `Workspace.EnergyPart` 带 `EnergyCapacityUpgrade` Tag。它必须是已锚定且允许碰撞、触碰的 `BasePart`，并包含 `SurfaceGui/TextLabel`。客户端按本地玩家的 `EnergyCapacityUpgradeLevel` 以英文显示下一次升级价格，因此多人看到的是各自价格；达到最高等级时显示 `MAX LEVEL`。玩家踏上时会立即尝试购买一次，之后只要仍站在该 Part 上，就按 `ShopConfig.EnergyCapacityUpgrades.AutoPurchaseIntervalSeconds` 的间隔持续自动购买；每次都由服务端根据最新容量升级等级重新计算价格。多个身体部件同时触碰不会创建重复购买循环，离开、死亡、退出、对象销毁或移除 Tag 时循环会停止。

容量升级已从商店界面和客户端购买操作中移除。客户端显示的价格不参与结算，也不能提交价格或等级；扣费、等级、容量刷新和存档均由 `PlayerShopService` 权威处理，结果通过玩家的 `StatusMessage` 显示。升级只提高上限，服务端会保留扣费前的当前能量（仅在数值超过有效上限时向下截断），不会把新增容量自动填满。服务端确认成功扣费后会向该玩家发送专用反馈事件，并播放 `ShopConfig.EnergyCapacityUpgrades.Feedback` 配置的本地收银机结算音效。

### 跑步机 Coins 与订阅解锁

跑步机根对象必须是带 `Treadmill` Tag 的 `Model`，内部包含名为 `Conveyor` 的 `BasePart`。模型名称使用 `x<number> Treadmill` 格式，服务端直接从名称解析倍率：`x1 Treadmill` 每秒奖励 1 Coins，`x50 Treadmill` 每秒奖励 50 Coins。玩家同时接触多台时只按最高的可用倍率结算，客户端不能提交倍率或 Coins 数值。

默认只有 `x1` 免费；`x3`、`x15`、`x50` 分别使用 Gold、Candy、Jade Experience Subscription，ID 统一配置在 `src/shared/SubscriptionConfig.luau`。目标月价分别为 500、998、1998 Robux；实际扣费与区域定价以 Creator Dashboard 为准，游戏脚本不能修改。需要场景级覆盖时，可在跑步机根模型设置 `TreadmillSubscriptionId = "EXP-..."`；旧的 `GamePassIdByMultiplier` 与 `TreadmillGamePassId` 仍作为永久权限兼容入口。订阅和 Game Pass 均未配置时，付费跑步机不会发钱或显示购买提示，并会输出配置警告。

```text
Workspace
└─ Treadmills
   └─ x15 Treadmill [Tag: Treadmill]
      ├─ TreadmillEnabled (Boolean): true
      ├─ TreadmillSubscriptionId (String): "EXP-3525910922356982237"
      └─ Conveyor (BasePart)
```

服务端按固定间隔检测 `Conveyor` 上方的存活角色，并通过 `GetUserSubscriptionStatusAsync` 缓存与 `Players.UserSubscriptionStatusChanged` 事件权威校验订阅状态，再调用统一 Coins 账户接口。付费跑步机会在 `Conveyor` 上生成 Roblox 原生 `ProximityPrompt`；玩家按 `E` 后客户端只负责调用 `PromptSubscriptionPurchase`，不能自行授予权限。取消、到期或状态变化后，权益会在服务器刷新状态时撤销。

服务端把玩家当前接触到的最高跑步机倍率同步到 `ActiveTreadmillMultiplier`；客户端据此播放该角色 `Animate` 中配置的跑步动画，离开、死亡或重生时停止并清理。如果角色没有可读取的自定义跑步动画，则按 R6/R15 使用 Roblox 默认跑步动画作为后备。动画只属于本地表现，不参与 Coins 权威结算；没有有效订阅或兼容 Game Pass 的玩家站上付费跑步机也会播放动画，但不会获得 Coins。

### 等级经验曲线（当前跑动加经验已停用）

以下字段位于 `src/shared/GameplayConfig.luau` 的 `GameplayConfig.Progression`：

| 字段 | 当前值 | 用途 |
| --- | ---: | --- |
| `StartingLevel` | `1` | 新存档起始等级 |
| `DistancePerExperienceAward` | `100` | 保留配置；当前 `PlayerEnergyService` 不再调用跑动经验结算 |
| `ExperiencePerDistanceAward` | `1` | 每次距离结算获得 1 经验 |
| `ExperienceRequiredByTargetLevel[2]` | `100` | 从 1 级升到 2 级所需经验 |
| `ExperienceRequiredByTargetLevel[3]` | `200` | 从 2 级升到 3 级所需经验 |
| `ExperienceRequiredByTargetLevel[4]` | `400` | 从 3 级升到 4 级所需经验 |
| `ExperienceRequiredByTargetLevel[5]` | `800` | 从 4 级升到 5 级所需经验 |
| `FallbackExperienceIncreasePercent` | `100` | 没有显式配置的后续等级，所需经验比前一级增加 100%，即翻倍 |
| `InitialExperienceRequired` | `100` | 经验表未提供 2 级数据时的初始后备值 |
| `DataStoreName` | `PlayerProgression_v1` | 等级、经验和未满一段的跑动距离所用 DataStore 名称 |
| `SaveIntervalSeconds` | `60` | 在线自动保存间隔 |

当前已停用玩家跑动加经验：`PlayerEnergyService` 不再调用 `PlayerProgressionService.addRunningDistance()`，客户端 `ProgressionHud` 也不会创建经验条。`PlayerProgressionService` 和已有存档字段仍保留，方便后续需要时重新接入或迁移。

经验反馈也在 `GameplayConfig.Progression.Feedback` 中配置：

| 字段 | 当前值 | 用途 |
| --- | ---: | --- |
| `ExperienceSoundId` | `rbxassetid://9113856580` | 获得跑动经验时播放的声音 |
| `SoundVolume` | `0.55` | 声音音量 |
| `PopupDurationSeconds` | `0.7` | 经验浮字持续时间 |
| `PopupRiseStuds` | `2.5` | 经验浮字上升距离 |

### 小兵视觉、拾取反馈与 Boss 表现配置

以下字段位于 `src/shared/GameplayConfig.luau`，属于客户端表现或服务端触碰容差配置：

| 配置路径 | 当前值 | 用途 |
| --- | ---: | --- |
| `MinionVisual.DefaultMode` | `Auto` | 跟随士兵默认视觉模式；有完整 R6 关节时自动播放跑步/下落动作，否则使用静态模式 |
| `MinionVisual.MaxAbsFacingYawDegrees` | `3600` | `FacingYawDegrees` 的绝对值上限 |
| `MinionVisual.MaxAbsGroundOffsetStuds` | `20` | `GroundOffsetStuds` 的绝对值上限 |
| `MinionPickup.Feedback.SoundId` | `rbxassetid://15004790432` | 拾取小兵时播放的本地音效 |
| `MinionPickup.Feedback.SoundVolume` | `0.6` | 拾取音效音量 |
| `Boss.TouchValidationDistance` | `12` | 角色根部距离 Boss 实际触碰 Part 包围盒的最大服务端容差（stud） |
| `Boss.ObjectiveRefreshSeconds` | `0.1` | 最近 Boss 目标提示刷新间隔；客户端最低钳制到 `0.02` 秒 |
| `Boss.Objective` | 见源码 | Boss 能量目标提示的显示距离、尺寸、颜色、脚底高度和脉冲参数 |
| `Boss.Fragments` | 见源码 | Boss 击败后碎片数量、尺寸、颜色、冲量和生命周期 |

### 玩家重生与出生保护

- `GameplayBootstrap.server.luau` 在服务启动前把 `PlayerRespawnSeconds = 0.5` 写入 `Players.RespawnTime`，其他服务不应重复实现自动重生计时。
- 修改重生间隔后，需要停止旧模拟并启动新的 Play/Test；运行中的服务端不会因 Rojo 同步自动重启。
- 场景中 `SpawnLocation.Duration` 应保持为 `0`，避免 Roblox 自动创建可见的 `ForceField` 出生保护球；新增出生点时，除非明确需要保护罩，也应使用 `0`。

### 安全上限

以下字段位于 `src/shared/GameplayConfig.luau` 的 `GameplayConfig.Limits`。它们主要用于服务端输入校验和防止异常配置，不是普通难度调节参数：

| 字段 | 当前值 | 限制对象 |
| --- | ---: | --- |
| `MaxPlayerLevel` | `1000` | 玩家最高等级 |
| `MaxEnergyValue` | `1,000,000` | 单个士兵模板战力及陷阱伤害校验上限 |
| `MaxBossRequiredEnergy` | `1,000,000` | Boss 门槛上限 |
| `MaxGameZoneIndex` | `1,000,000` | GameZone 编号上限 |
| `MaxForcedRunSpeed` | `200` | GameZone 强制前进速度上限 |
| `MaxMinionsPerGameZone` | `1000` | 单区为单名玩家生成的拾取物上限 |
| `MaxFollowersPerPlayer` | `1000` | 单名玩家的真实跟随士兵上限 |

### 士兵数量门与随机门

以下字段位于 `src/shared/MinionModifierConfig.luau`：

| 字段 | 当前值 | 用途 |
| --- | ---: | --- |
| `MaxOperand` | `1000` | `Add`、`Subtract` 操作数上限 |
| `MaxMultiplier` | `10` | `Multiply` 倍数上限 |
| `TouchDistance` | `6` | 角色根部距离门包围盒的最大服务端容差（stud） |
| `RetryCooldownSeconds` | `1` | 同一玩家对同一门/门组的失败重试冷却 |
| `AssignmentRequestCooldownSeconds` | `0.05` | 客户端查询个人随机门面的服务端限流 |
| `RandomGroup.PositiveChanceForExtraSlots` | `0.5` | 除保证的一正一负以外，其余门生成正面效果的概率 |

随机门效果池也在同一个脚本中：

```lua
PositiveEffects = {
    { operation = "Add", operand = 3 },
    { operation = "Add", operand = 5 },
    { operation = "Multiply", operand = 2 },
}

NegativeEffects = {
    { operation = "Subtract", operand = 3 },
    { operation = "Subtract", operand = 5 },
}
```

修改效果池会影响所有 `RandomMinionModifierGroup`。程序仍会保证每组至少出现一个正面和一个负面效果。

### 仍位于具体实现脚本的常量

以下常量尚未集中到共享配置，适合在需要调整对应表现或生成细节时到指定脚本修改：

| 脚本 | 当前常量/数值 | 用途 |
| --- | --- | --- |
| `src/server/Services/PlayerEnergyService.luau` | GameZone 垂直容差 `8` studs | 允许角色根部位于区域包围盒上方一定高度 |
| `src/server/Services/PlayerEnergyService.luau` | 移动采样上限 `32 × deltaTime` | 限制单帧可计入战力和 Coins 的距离，避免传送被当成跑动 |
| `src/server/Services/PlayerEnergyService.luau` | `MoveDirection >= 0.05`、移动距离 `> 0.01` | 判定玩家是否处于有效跑动状态 |
| `src/server/Services/MinionSpawnService.luau` | `SPAWN_EDGE_PADDING = 3`、`SPAWN_SURFACE_OFFSET = 0.05`、`MAX_POSITION_ATTEMPTS = 24` | 拾取物生成边距、贴地偏移与随机位置尝试次数 |
| `src/server/Services/MinionService.luau` | `SOURCE_DESTROY_DELAY = 2` | 被击飞士兵来源对象的销毁延迟 |
| `src/client/MinionPresentation.client.luau` | `FORMATION_*`、`FOLLOW_*`、`RUN_*`、`JOIN_*`、`KNOCKBACK_*` | 小兵队形、跟随、跑步、入队及击飞表现；源码旁已有逐项中文注释 |
| `src/client/EnergyHud.client.luau` | `CAMERA_PULL_DISTANCE = 7.5`、`CAMERA_FOV_INCREASE = 7`、三个阶段时长 `0.28/0.42/0.55` 秒 | Boss 撞击镜头表现 |

## `GameZone`

`GameZone` 的包围盒定义可跑动充能的区域。玩家处于区域内并移动时获得能量和 Coins；停下时消耗能量。进入区域后，服务端为当前区及后续两个区预生成玩家专属士兵拾取物。

| Attribute | 类型 | 必填 | 范围或默认值 | 用途 |
| --- | --- | --- | --- | --- |
| `ZoneIndex` | Number（整数） | 是 | 0～1,000,000 | 区域流程编号；用于设置玩家当前区域并决定预生成哪些区的拾取物 |
| `MinionSpawnCount` | Number（整数） | 否 | `-1`，或 1～1,000；默认 `10` | `-1` 禁止该区域生成拾取物；正数是每名玩家的生成数量 |
| `ForcedRunSpeed` | Number | 否 | 不设置或 `0` 表示关闭；启用值大于 0 且不超过 200 | 玩家进入区域后的固定前进速度 |
| `ForcedRunDirection` | Vector3 | `ForcedRunSpeed > 0` 时是 | X/Z 水平分量不能同时为 0；Y 分量忽略 | 强制前进的世界方向 |

补充规则：

- 玩家只在区域内累计有效跑动距离；每满 10 studs 默认结算 1 能量，未满一段的距离会留到后续跑动。
- 玩家在区域内静止时，默认每秒消耗 `5` 能量。
- 区域高度判定会在包围盒上下额外容许 8 studs。
- `ZoneIndex` 无效时，该对象不会启用 `GameZone` 行为。
- `MinionSpawnCount = -1` 时，该区域不会创建预置或随机拾取物，也不会创建空生成文件夹，并会被记为本条命已处理。
- 除 `-1` 与 1～1,000 整数以外的 `MinionSpawnCount` 会警告，并退回默认值 `10`。
- 启用强制前进后，服务端只约束配置方向上的速度分量；玩家仍可跳跃、下落和横向移动，但无法沿反方向退回。离开区域、死亡或配置失效时约束会清理。
- `ForcedRunSpeed` 与 `ForcedRunDirection` 支持运行时热更新；`ZoneIndex` 同时被其他区域服务缓存，修改后应重新添加 `GameZone` Tag 或重启 Play/Test。

服务器启动时，服务会收集 `GameZone` 后代中已经带 `MinionPicker` Tag 的有效对象作为固定位置预置。预置原件仅作为位置和外观原型，随后会被移出 DataModel；每名玩家、每条命都会获得带 `SpawnOwnerUserId` 的独立副本。

- 预置必须是 `BasePart` 或包含 `BasePart` 的 `Model`；`MinionType` 不设置或为空字符串时，每名玩家的运行时副本随机选择一个已解锁兵种，非空时必须精确匹配模板名称。
- 有效预置数小于 `MinionSpawnCount` 时，仅随机补足差值；例如目标 10、预置 4 个时，再随机生成 6 个。
- 预置数大于目标时保留全部预置，不再随机补充；超额提示只在 Studio 输出。
- 随机选点会把预置位置计入 `MinionSpawnMinSpacing` 检查，避免与固定拾取物重叠。
- 玩家死亡、角色移除或离开服务器时会清理其运行时拾取物；重生后按新的一条命重新创建。

示例：

```text
Tag: GameZone
ZoneIndex (Number): 0
MinionSpawnCount (Number): 10
ForcedRunSpeed (Number): 28
ForcedRunDirection (Vector3): 0, 0, 1
```

不希望某个区域生成拾取物时：

```text
Tag: GameZone
ZoneIndex (Number): 1
MinionSpawnCount (Number): -1
```

## `Minion`

`Minion` 是士兵模板发现机制的旧版兼容 Tag，并不是 Workspace 中的可拾取士兵 Tag。

推荐结构是在 `ServerStorage` 下创建 `MinionTemplates` 文件夹，把每种士兵模板作为其直接子对象；使用该结构时，文件夹内每个有效直接子对象都会被读取，不需要添加 `Minion` Tag。

只有不存在 `ServerStorage/MinionTemplates` 时，系统才会在 `ServerStorage` 的直接子对象中读取带 `Minion` Tag 的模板。服务启动并缓存模板后会移除模板上的 `Minion` 和 `MinionPicker` Tag，防止模板被当成游戏对象处理。

| Attribute | 类型 | 必填 | 范围 | 用途 |
| --- | --- | --- | --- | --- |
| `EnergyValue` | Number | 是 | 1～1,000,000 | 玩家拾取该兵种时增加的能量，也是该士兵在陷阱或 Boss 击飞结算中代表的能量值 |
| `EnergyUpgradePerLevel` | Number（整数） | 是 | 1～1,000,000 | 该人偶在商店每购买 1 级能量升级时增加的能量；可按人偶分别配置 |
| `MinionIconId` | String 或 Number（整数） | 是 | 纯数字 Roblox 图片资源 ID | 该人偶在商店卡片中显示的图标；例如资源地址为 `rbxassetid://123456` 时填写 `123456` |
| `MinionVisualMode` | String | 否 | `Auto`、`Animated`、`Static`；默认 `Auto` | 控制客户端跟随表现是否尝试播放内置 R6 跑步/下落动作 |
| `FacingYawDegrees` | Number | 否 | `-3600..3600`；默认 `0` | 纠正模板朝向，让士兵在队形中面向合适方向 |
| `GroundOffsetStuds` | Number | 否 | `-20..20`；默认 `0` | 调整模型贴地高度，避免悬浮或陷入地面 |

模板还必须满足：

- 是 `BasePart`，或是包含至少一个 `BasePart` 的 `Model`。
- `Archivable = true`，因为服务端需要克隆模板。
- 模板名称在容器中唯一；名称会被 `MinionType` 精确引用。
- 小兵视觉 Attribute 会随模板克隆到 `ReplicatedStorage/MinionFollowerStorage` 的来源对象，客户端只读取来源对象上的值；运行中修改模板不会影响已经生成的跟随士兵。

示例：

```text
ServerStorage
└─ MinionTemplates
   └─ Soldier
      ├─ EnergyValue (Number): 10
      ├─ EnergyUpgradePerLevel (Number): 2
      └─ MinionIconId (String): "123456789"
```

## `MinionPicker`

`MinionPicker` 通常由 `MinionSpawnService` 在运行时创建，也可以作为 `GameZone` 后代预置，用来指定固定位置、外观和兵种。玩家只有在 `GameZone` 内才可以拾取；若拾取物带有所有者限制，则只有对应玩家可以拾取。成功拾取后，它会从世界中移除，并创建跟随士兵。

| Attribute | 类型 | 来源 | 范围或默认值 | 用途 |
| --- | --- | --- | --- | --- |
| `MinionType` | String | 运行时写入；手工预置可选 | 预置缺省/空字符串时随机选择已解锁兵种；非空时精确匹配模板名称 | 决定加入队伍的士兵模板及其 `EnergyValue` |
| `SpawnOwnerUserId` | Number | 服务端运行时 | 玩家 `UserId`；缺省时不限制所有者 | 限制拾取物只能由指定玩家拾取 |
| `Collected` | Boolean | 服务端运行时 | 默认 `false` | 防止身体多个部位同时触碰造成重复领取 |

补充规则：

- 项目用名为 `pickBall` 的 `BasePart` 或 `Model` 作为拾取物外观模板，优先从 `ServerStorage` 查找，也兼容 Workspace 中的模板。`pickBall.MinionType` 是可选的全局兵种锁定：不设置或设为空字符串时，每个随机拾取物从 `MinionTemplates` 随机选择；填写有效模板名时，所有随机拾取物都使用该兵种。填写的名称无效时会警告并停用随机拾取物生成。
- 手摆预置必须位于某个 `GameZone` 的后代层级；服务器启动后会把原件转换成每名玩家、每条命独立的运行时副本。预置 `MinionType` 留空时，每名玩家分别随机一个已解锁兵种。
- 每名玩家最多拥有 1,000 个跟随士兵；满员时拾取物不会被消耗。
- 拾取后获得士兵模板的 `EnergyValue` 能量，但能量仍受玩家容量限制。

## `MinionModifier`

`MinionModifier` 是 Workspace 中的功能 Part。玩家碰触后，服务端对真实跟随士兵队列执行加、减或乘运算，并显示对应的世界文字。

对象要求：

- 必须是 `BasePart`，不能把 Tag 放在外层 `Model` 上。
- `Anchored = true`。
- `CanTouch = true`。
- 通常设置 `CanCollide = false`，让玩家穿过它。

| Attribute | 类型 | 必填 | 范围或默认值 | 用途 |
| --- | --- | --- | --- | --- |
| `Operation` | String | 是 | `Add`、`Subtract`、`Multiply` | 选择加法、减法或乘法 |
| `Operand` | Number（整数） | 是 | 加减：1～1,000；乘法：0～10 | 运算使用的数量或倍数 |
| `Enabled` | Boolean | 否 | 默认 `true` | 为 `false` 时暂停触发并隐藏运算文字 |

常用配置：

| 效果 | `Operation` | `Operand` |
| --- | --- | --- |
| 士兵 -2 | `Subtract` | `2` |
| 士兵 +4 | `Add` | `4` |
| 士兵 ×2 | `Multiply` | `2` |

补充规则：

- 每名玩家每条命对同一个 Part 只触发一次；不同玩家互不影响，重生后可以再次触发。
- 数量结果限制为 0～1,000。
- 减法从队尾移除士兵，并使用已有击飞表现。
- 加法和乘法产生的新士兵，全部复制触发前玩家跟随队列中序号最后一个士兵的兵种、外观和战力数值。
- 队伍为空时，加法使用第一个有效且已解锁的模板；0 名士兵乘任何数仍是 0。
- 有效但结果不变的运算也会消耗本条命的使用机会。
- 士兵数量变化会同步改变当前战力：新增士兵按模板 `EnergyValue` 增加，移除士兵按实际被移除兵种的 `EnergyValue` 扣除；最终仍受战力容量限制。
- Part 不要求位于 `GameZone` 内。

完整配置说明也可查看 `docs/MinionModifier.md`。

## `RandomMinionModifierGroup`

`RandomMinionModifierGroup` 用于让程序为一组横向门生成玩家专属效果。Tag 必须放在 Workspace 中的根 `Model` 上，Model 至少包含两个直接子级 `BasePart`。

门 Part 要求：

- `Anchored = true`。
- `CanTouch = true`。
- 推荐 `CanCollide = false`。
- 不添加 `MinionModifier` Tag，也不需要 `Operation`、`Operand`、`MinionType` Attribute。

所有直接子级 BasePart 都会被当作门槽；装饰 Part 应放进下一级 Model 或 Folder。门组自身只要求位于 Workspace；触发时要求玩家当前位于有效 `GameZone` 内。

每名玩家、每次出生都会获得独立随机结果，同组始终至少包含一个正面和一个负面效果。当前角色选择一扇后，同组其他门立即失效并隐藏；重生后重新随机并再次开放。具体效果池在 `src/shared/MinionModifierConfig.luau` 的 `RandomGroup` 中配置。

## `Trap`

`Trap` 是权威服务端处理的能量陷阱。玩家碰触后最多损失配置的能量值，并按实际损失能量从队尾释放跟随士兵。每名玩家对每个陷阱有 1 秒服务端冷却，防止多个身体部位造成重复结算。

| Attribute | 类型 | 必填 | 范围 | 用途 |
| --- | --- | --- | --- | --- |
| `EnergyDamage` | Number | 是 | 1～1,000,000 | 单次触碰尝试扣除的能量值 |

如果玩家当前能量低于 `EnergyDamage`，只扣到 0；如果没有能量，则不击飞士兵。无效配置的陷阱不会启用。

示例：

```text
Tag: Trap
EnergyDamage (Number): 20
```

## `KillZone`

`KillZone` 是服务端权威的即死触碰区域，只接受 Workspace 中的 `BasePart`，不需要任何 Attribute。玩家角色的任意身体部件触碰后，当前 `Humanoid.Health` 会立即设为 `0`；NPC 与其他非玩家模型不受影响。

- 推荐设置 `Anchored = true`、`CanTouch = true`。
- `CanCollide` 由玩法决定：实体钉刺或路障通常为 `true`，隐形坠落区通常为 `false`。
- 多个 Part 组成的区域需要给每个负责触碰判定的 Part 添加 `KillZone` Tag；纯装饰 Part 不需要添加。

```text
Tag: KillZone
Attributes: 无
```

## `Boss`

场景中带 `Boss` Tag 的对象是生成模板。服务启动后会将模板移出 DataModel，并为每名玩家、每条命创建独立 Boss 副本。客户端只显示属于自己的 Boss，其他玩家的副本不可见且不能触发。

玩家碰触自己的 Boss 时，服务端检查能量是否达到门槛。能量足够时扣除门槛能量、击飞对应士兵并摧毁该副本；不足时玩家死亡。若扣除后能量恰好为 0，Boss 会被击败，但玩家也会死亡。

| Attribute | 类型 | 必填 | 范围或默认值 | 用途 |
| --- | --- | --- | --- | --- |
| `RequiredEnergy` | Number | 是 | 1～1,000,000 | 击败 Boss 所需并实际扣除的能量 |
| `BossOwnerUserId` | Number（整数） | 服务端运行时 | 所属玩家 `UserId` | 限制触发者，并供客户端隐藏其他玩家的 Boss |
| `Defeated` | Boolean | 服务端运行时 | 默认 `false` | 标记 Boss 是否已进入击败流程，防止重复结算 |

服务会在 Boss 主 Part 上自动生成能量门槛文字。Boss 被击败后不会定时恢复；玩家重生时会清理该玩家上一条命的全部 Boss，并从场景模板统一创建新副本。模板必须位于 Workspace、支持克隆（`Archivable = true`），且为 `BasePart` 或包含 `BasePart` 的 `Model`。

示例：

```text
Tag: Boss
RequiredEnergy (Number): 100
```

## 无 Tag 的运行时 Attribute

以下 Attribute 也定义在项目公共配置中，但它们不是场景 Tag 的人工配置。它们由服务端写入 Player、跟随士兵来源对象或运行时容器，供其他服务和客户端表现读取。

升级反馈通过服务端创建的 `ReplicatedStorage/ProgressionFeedback` RemoteEvent 发送。它不是 Tag，也没有项目自定义 Attribute；客户端只接收服务端发送的经验值与升级数量。

### Player Attribute

| Attribute | 类型 | 典型初始值 | 用途 |
| --- | --- | --- | --- |
| `PlayerLevel` | Number（整数） | `1` | 玩家等级；范围 1～1,000；当前不再决定能量容量 |
| `CurrentExperience` | Number（整数） | `0` | 当前等级已积累经验 |
| `ExperienceToNextLevel` | Number（整数） | `100` | 升到下一级所需经验；满级时为 0 |
| `Coins` | Number（整数） | `0` | 商店游戏币，由服务端通过有效跑动距离和商店消费维护 |
| `EnergyCapacityUpgradeLevel` | Number（整数） | `0` | 在 `EnergyCapacityUpgrade` 区域购买的能量上限升级等级 |
| `ShopRevision` | Number（整数） | `0` | 商店状态版本号；客户端和小兵服务据此刷新展示与现有 follower 能量 |
| `CurrentEnergy` | Number | `0` | 当前能量，由服务端维护并限制在 0～容量 |
| `EnergyCapacity` | Number | `120` | 当前能量上限；公式为基础容量加商店容量升级加成 |
| `IsInGameZone` | Boolean | `false` | 玩家当前是否位于有效 `GameZone` |
| `CurrentGameZoneIndex` | Number（整数）或不存在 | 不存在 | 当前区域的 `ZoneIndex`；离开区域时清除 |
| `ForcedRunActive` | Boolean | `false` | 当前角色是否受服务端强制前进约束；客户端据此屏蔽后退输入 |
| `ActiveTreadmillMultiplier` | Number（整数） | `0` | 服务端检测到的当前最高跑步机倍率；`0` 表示未站在跑步机上，客户端据此播放或停止跑步动画 |
| `FollowerCount` | Number（整数） | `0` | 当前真实跟随士兵数量，范围 0～1,000 |
| `StatusMessage` | String | 未设置 | 服务端发给 HUD 的最近一条状态提示 |
| `BossImpactSequence` | Number（整数） | `0` | 成功撞击 Boss 后递增，客户端据此播放一次撞击反馈 |

等级、经验和未结算的经验距离仍可通过 DataStore 保存，但当前跑动加经验与经验条显示已停用。能量、Coins、区域状态、跟随士兵数量、状态提示和 Boss 撞击序号由对应服务维护。

### 跟随士兵来源对象

跟随士兵的服务端来源对象保存在 `ReplicatedStorage/MinionFollowerStorage/<UserId>` 下，不带场景行为 Tag。客户端读取这些 Attribute 创建和维护本地跟随表现。

| Attribute | 类型 | 用途 |
| --- | --- | --- |
| `FollowerOwnerUserId` | Number | 标识士兵所属玩家；也写在该玩家的来源文件夹上 |
| `FollowerOrder` | Number（整数） | 士兵在跟随队形中的稳定顺序 |
| `FollowerState` | String | `Following` 表示正常跟随，`Knockback` 表示播放击飞，`Removed` 表示直接移除 |
| `MinionVisualMode` | String | 可从士兵模板继承；控制客户端使用 `Auto`、`Animated` 或 `Static` 跟随表现 |
| `FacingYawDegrees` | Number | 可从士兵模板继承；客户端用于修正士兵朝向 |
| `GroundOffsetStuds` | Number | 可从士兵模板继承；客户端用于修正贴地高度 |

### 拾取物生成容器

`MinionSpawnService` 在 Workspace 下创建 `MinionSpawnSessions`，并为每名玩家、每个区域创建文件夹。文件夹使用：

| Attribute | 类型 | 用途 |
| --- | --- | --- |
| `SpawnOwnerUserId` | Number | 标识该生成文件夹所属玩家 |
| `ZoneIndex` | Number（整数） | 标识该文件夹对应的区域 |

这些容器及其中的拾取物会在角色死亡、角色移除或玩家离开时清理。

## 配置注意事项

- Attribute 名区分大小写，统一使用本文列出的 PascalCase 名称。
- Number 类型中的“整数”必须没有小数部分。
- 无效 Tag 类型或无效必填 Attribute 会产生带服务模块前缀的警告，并且对象不会启用对应行为。
- `GameZone`、`MinionPicker`、`RandomMinionModifierGroup`、`Trap`、`KillZone`、`Boss` 支持运行前已有对象和运行时新增 Tag，也会在 Tag 移除时解绑。
- `MinionModifier` 同样支持运行时新增、移除和配置更新。
- `RandomMinionModifierGroup` 会监听直接子级门槽以及 `Anchored`、`CanTouch` 的变化；`ForcedRunService` 会监听 `ForcedRunSpeed` 与 `ForcedRunDirection`。其他已绑定对象的配置 Attribute 通常不会热更新，运行时修改后应重新添加对应 Tag，或重新启动 Play/Test。
- `Minion` 模板只在 `MinionSpawnService` 启动时缓存；运行中新增模板不会自动进入当前模板列表。
- 场景对象不需要各自放 Script；统一服务根据 Tag 绑定行为。

## level5～9 场景装饰

`Assets/DecorateLevels5To9.luau` 为当前 Place1 布局的编辑态重建脚本，不参与 Rojo 运行时代码同步。门楼和平台装饰位于 `Workspace/PetCastleDecor/07_Levels5To9/Level5`～`Level9`；赛道两旁的大树、浮岛和宠物雕像位于 `Workspace/PetCastleDecor/TracksideDecor/07_Levels5To9/Level5`～`Level9`。前段关卡的 `03_BlockGardens` 和 `06_PetLookouts` 也统一收纳在 `TracksideDecor` 中。

每关包含同款红蓝金城堡门楼、4 棵大型积木树浮岛和 1 只宠物雕像。平台金边与爪印按实际 `ground` 范围生成，不跨越跳台缺口。爪印复用图片 `11767069582`，每枚仅使用一个透明 Part 和 Decal。新增 Part 均锚定并关闭碰撞、触摸和查询，不含脚本或玩法标签。

仅调整这些关卡的地面、水面和墙面外观，已有 Texture 透明度保持为 `0.85`。脚本会检查原有对象的位置、尺寸、透明度、碰撞及标签和属性未被改变。重跑仅替换上述两个 `07_Levels5To9` 文件夹，会覆盖其中的手动调整；需要恢复原配色时请使用 Studio Undo 或场景备份。完成场景编辑后在 Studio 保存 Place 文件。
